# Sigma bot environment variables
# Copy this file to .env and fill in the values

# Required for live trading modes (--live, --maker, --claim)
DEPLOYER_PRIVATE_KEY=

# Optional: override defaults
SIGMA_MIN_EDGE_BPS=200
SIGMA_MAX_STAKE=25
AUTO_CLAIM=false
